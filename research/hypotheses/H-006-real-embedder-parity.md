
# H-006: 换真嵌入的平价复核——结构无关结论是否迁移（EXP-006 假设）

- **状态**: UNVALIDATED（2026-08-19 注册，先于任何 EXP-006 运行）
- **日期**: 2026-08-19
- **关联**: EXP-001（原基线）、EXP-003（单格点显著性）、ADR-0003（GPU 路径）

## 假设

把语料从「合成恒等嵌入」换成「真文本碎片 + GPU 嵌入模型（BGE-M3 fp16）」，
EXP-001/003 已确立的三条**结构无关结论**应当迁移:

- **H-006a**: A（平铺语义 + 来源加权）仍是质量基线（F1@k / NDCG@k /
  Recall@k 领先或差距不显著）；
- **H-006b**: B（锚点）仍省算力（sim_calls 节省 ≥ 2×），且其召回损失
  模式不变（存在、不随单一歧义轴收敛——H-001 遗留问题的真嵌入版）；
- **H-006c**: C（多网渐进验证）早停机制仍有效（≥ 50% 查询提前终止）；
- **H-006d**（对照 EXP-003）: C vs A 在 tpe=4 / noise=0.30 格点的
  F1@10 差仍过四闸门（或如实记 REFUTED——单格点结论本就允许不迁移）。

## 可证伪条件

四个分量各自独立三态判定（SUPPORTED / REFUTED / INCONCLUSIVE），
不做复合投票；任一分量 REFUTED 即如实记录「该结论仅合成嵌入成立」。

## 不回答的问题

真嵌入是否让 C 全面超过 A（那是 EXP-007+ 的规模线问题）；
排序器/重排器方向（候选 H-007，另行注册）。
