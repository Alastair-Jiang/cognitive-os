## LOG-2026-08-20-exp004a — EXP-004a Oracle Headroom 运行(G1 PASS)

- 状态: 完成
- 关联: EXP-004 预注册(冻结), H-005, E1 协议层(2fbbe42), E0.5 文本语料(前段)

### Problem
预注册 R2: 实现 run_exp004a_oracle.py, 跑语料宇宙 × 3 seed,
按预注册定义测 Headroom-H0/H1, 回填 G1 判定。运行器须 import 复用
EXP-002 的策略模板与网格(不复制, D-5), 判定只在 decide() 拼装。

### Hypothesis
预注册 G1: 主 λ=0.02 下, 池化 H1 ≥ 0.03 且 ≥3 格格内 H1 ≥ 0.02 →
存在查询级自适应空间, H-005 进入 004b; H1 不达但 H0 ≥ 0.03 → regime 级
改写; 均不达 → H-005 REFUTED。

### Change
- run_exp002_scan.py: 抽出 grid_corpus_config() 工厂(进程内验证
  与原内联配置逐字段相等, 零漂移), run_cell 改用工厂;
- 新增 scripts/run_exp004a_oracle.py: 语料宇宙(9 网格 + medium,
  共 10 个独立配置)×3 seed×12 查询×A/B/C, 逐查询 F1/sim_calls 落盘,
  λ 全扫描入汇总, G1 三态判定(含 EDGE 边界分支如实标注);
- 新增 tests/test_run_exp004a_oracle.py 9 测试(宇宙/工厂零漂移/
  冻结常数/效用公式/聚合公式手算对照/判定分支/实跑记录);
- 文档: 预注册状态行与结果节回填, 说明书表 A 回填, 测试计数 113。

### Experiment
python scripts/run_exp004a_oracle.py(seed 20260819,7,42; 每配置
12 查询, query_seed=1, k=10; 策略模板冻结 import)。意外双跑(同一命令
起了两个后台作业): 两次输出逐数字一致(确定性对账), 重复结果文件已删,
保留先落地的一份。

### Result
- U_gf=0.7100(A-traditional), H0 池化 +0.0358, H1 池化 +0.0408;
- 格内 H1 ≥ 0.02 的格数 8/10(仅 overlap-low-noise-low +0.0099 与
  overlap-mid-noise-low +0.0052 不达; 两格恰是 C 大幅领先的格,
  事后最优已接近天花板);
- 逐 seed 池化 H1(主 λ): 20260819→+0.0373, 7→+0.0344, 42→+0.0425(全正);
- λ 扫描: H1 在 λ∈{0,0.01,0.02,0.05} 为 0.0344/0.0386/0.0408/0.0369,
  λ=0.1 时降至 0.0189(成本权重放大后查询级空间收缩, 如实记录);
- **G1 verdict: PASS**(g1a/g1b/g1r 全过, 边界旗标未触发);
- 语料宇宙口径备注: 预注册写「共 11 个」, 实际独立参数配置 10 个
  (网格 3×3=9 + medium; small 与 overlap-mid-noise-high 参数全同),
  已在结果 JSON meta 与预注册结果节如实标注; G1 阈值按原值执行
  (至少 3 格, 3/10 较 3/11 更严);
- 门禁: pytest 113/113(104+9), 卫生扫描 126 文件 0 异常;
- 结果: research/results/EXP-004a-oracle-s3-q12-20260820-025839.json。

### Interpretation
查询级自适应空间存在且幅度可观(池化 ~0.04U, 约为 A-vs-C 典型 F1 差的
1/3)。8/10 格有逐查询选择空间, 例外两格恰是"单一策略已近最优"的格。
按预注册, 下一步进入 004b(状态可测性: f1/f2/f3 廉价特征 × 逐查询最优
策略标签, 训练/检验半区防泄漏)。regime 级空间(H0=+0.0358)同样存在
但小于查询级, 004b 若特征可测则查询级路线优先。

### Next Step
1. EXP-004b 特征检验运行器(f1/f2/f3, Bonferroni 校正, 半区防泄漏);
2. E2 三前置(SearchNet 引用通道 / 成本口径 / 有序路径恢复 /L);
3. EXP-005 / EXP-006 运行器。
